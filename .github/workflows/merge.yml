name: merge

on:
  push:
    branches:
      - master

jobs:
  upload:
    if: github.repository == 'cvmfs-contrib/config-repo'
    uses: ./.github/workflows/apply-corrections.yml
    with:
      skip-unchanged: true
      upload-db: true
      release-db: true
    secrets: inherit
